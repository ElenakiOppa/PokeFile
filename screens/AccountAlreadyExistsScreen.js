import React from 'react';
import FigmaAuthStatus from '../components/FigmaAuthStatus';
export default function AccountAlreadyExistsScreen({ navigate, goBack, params }) { const email = params?.email || 'this email'; return <FigmaAuthStatus eyebrow="REGISTRY CONFLICT" title="Account Exists" icon="information-outline" heading="Profile Already Active" message={`An official Pokéfile profile has already been verified under ${email}. Sign in to access your vault index.`} primaryLabel="Sign In Instead" onPrimary={() => navigate('SignIn')} secondaryLabel="Reset Vault Password" onSecondary={() => navigate('ForgotPassword')} onBack={goBack} />; }
